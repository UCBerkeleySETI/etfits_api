NOTE : This copy of the code should be considered archival.  The working
copy is now on source forge as etfits-api.


To compile C code: 
make
To compile Python wrapper
swig -python -c++ -extranative s6fits.i
make _s6fits.so

